<div class="card-toolbar custom-toolbar ms-auto {{ !$makeVcard ? 'd-none' : ''}}">
        <div class="d-flex justify-content-end" >
            <a type="button" class="btn btn-primary" href="{{ route('vcards.create')}}">{{__('messages.rcard.new_rcard')}}</a>
        </div>
    </div>

